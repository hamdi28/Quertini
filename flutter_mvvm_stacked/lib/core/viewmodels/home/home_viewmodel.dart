import 'package:stacked/stacked.dart';

class HomeViewModel extends IndexTrackingViewModel {
  // IndexTrackingViewModel already provides selectedIndex and setIndex functionality
  // No further logic needed for now for basic navigation
}
